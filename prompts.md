[] create a script (bash or python, will be running on macos) named protonvpn-plublic-port-refresh that refreshes the protonvpn public port every --refresh-seconds. My current working version is in folx-public-port-orig.sh. feel free to write as you see fit. command line option for --vpn-gateway defaults to 10.2.0.1 for ProtonVPN. a parameter --app-control default to "". this is an optional paramter to be able to console specific applications. create a configuration section. define an applicatinon named Folx3-setapp. Each application shouls have a path to the application and a defaults string and a start and stop function. the natpmp needs refreshed every --refresh-seconds defaulting to 45 seconds. allow refresh to stop at any time. With first successful refresh, and one or more  appications are being controlled, run the start function for each application. when we stop for any reason, run the stop function for each application. nove all static variabled to the configuration settings. create a nice logging output. add --loglevel for info, warning, error default to info